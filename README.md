# led-ticker-calendar — moved

This plugin now lives in the **[led-ticker-plugins](https://github.com/JamesAwesome/led-ticker-plugins)** monorepo at **[`plugins/calendar/`](https://github.com/JamesAwesome/led-ticker-plugins/tree/main/plugins/calendar)**.

Install it from there:

```
git+https://github.com/JamesAwesome/led-ticker-plugins.git@calendar-v0.1.0#subdirectory=plugins/calendar
```

The widget type (`calendar.events`) is unchanged. This repository is archived (read-only); its history is preserved in the monorepo.
